integrity
    correctness
        数据符合真实世界语义
    compatibility
        同一对象在不同关系中的数据符合逻辑
    1。 定义完整性约束的机制
    2。 每次操作后的检查
    3。 违约处理
实体完整性
    1. 列级
        CREATE TABLE Student(
            Sno CHAR(9) PRIMARY KEY
        );
    2. 表级
        CREATE TABLE Student(
            Sno CHAR(9),
            Cno CHAR(9),
            PRIMARY KEY(Sno, Cno)
        );
