CREATE TABLE Student(
    Sno CHAR(20) CONSTRAINT C1 CHECK(Sno BETWEEN 90000 AND 99999),
    Sname CHAR(20) CONSTRAINT C2 NOT NULL,
    SAGE CHAR（20) CONSTRAINT C3 CHECK(Sage<30),
    CONSTRAINT StudentKey PRIMARY KEY(Sno)
)

ALTER TABLE Student
DROP CONSTRAINT C4;